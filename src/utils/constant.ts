export const DEFAULT_PAGE_SIZE = 10;

export const DEFAULT_PAGE_SIZE_OPTIONS = [10, 20, 30, 40, 50];

// Mainly for component that do not have pagination implementation (e.g. dropdowns)
export const DEFAULT_NON_PAGINATE_PAGE_SIZE = 50;

// customize workflow run page
export const WORKFLOW_RUN_DEFAULT_PAGE_SIZE = 10;
export const WORKFLOW_RUN_PAGE_SIZE_OPTIONS = [10, 20, 30, 50];
